# Augur.net Optimization Plan

## Overview

This document outlines optimization tasks for the augur.net codebase. Tasks are organized by priority and effort, with dependencies noted where applicable.

---

## Priority 1: Quick Wins (Low Effort, High Impact) ✅ COMPLETE

### [DONE] 1.1 Fix Biome Lint Errors
**Files:** Multiple
**Effort:** ~10 min
**Status:** [x]

| # | File | Issue | Status |
|---|------|-------|--------|
| 1.1a | `src/providers/ForkDataProvider.tsx:125` | Remove useless `case 'unknown':` - default handles it | ✅ |
| 1.1b | `src/stores/animationStore.ts:25-26` | Use template literals instead of string concatenation | ✅ |
| 1.1c | `src/components/ForkDisplay.tsx:9` | Remove unused `cn` import | ✅ |
| 1.1d | `src/components/ForkMonitor.tsx:10` | Prefix unused params with `_` | ✅ |

**Additional lint issues found (not in original list):**
- `ForkStats.tsx` - unused `cn` import + unused props (prefixed with `_`)
- `LearnNavigation.tsx` - unused React import (removed)
- `PerspectiveGridTunnel.tsx` - unused variable (prefixed with `_`)

**Note:** Remaining lint warnings are in `worker-configuration.d.ts` (auto-generated by Wrangler) - not actionable.

**Fixed:** Added `worker-configuration.d.ts` to `.gitignore` - Biome now ignores it via VCS integration.

---

### [DONE] 1.3 Remove Dead Code
**Effort:** ~10 min
**Status:** [x]

Cleaned up unused code from lint fixes:

| # | File | Change |
|---|------|--------|
| 1.3a | `ForkStats.tsx` | Removed unused props - component now uses only context |
| 1.3b | `ForkDisplay.tsx` | Removed unused props from `<ForkStats />` JSX, removed unused `riskLevel` destructure |
| 1.3c | `PerspectiveGridTunnel.tsx` | Removed unused `horizonWidth`, `horizonHeight` variables |

**Note:** `ForkMonitor.tsx` still has `animated` prop - kept because it's passed from `HeroBanner.astro` (API compatibility).

---

### [DONE] 1.2 Remove Duplicate CSS Keyframes
**File:** `src/styles/global.css:443-478`
**Effort:** ~5 min
**Status:** [x]

`@keyframes keycap-glow` is defined twice. Removed the first duplicate (lines 443-460).

---

## Priority 2: Performance (Medium Effort, High Impact)

### [ ] 2.1 React Component Optimization
**Effort:** ~30 min
**Status:** [ ]
**Depends on:** 1.1

| # | Component | Optimization |
|---|-----------|--------------|
| 2.1a | `ForkGauge.tsx` | Wrap expensive calculations in `useMemo` (`getVisualPercentage`, `getRiskColor`, `getRiskLevel`, SVG paths) |
| 2.1b | `ForkDataProvider.tsx` | Memoize `contextValue` object to prevent unnecessary re-renders |
| 2.1c | `ForkGauge.tsx` | Add React.memo to prevent re-renders when data hasn't changed |

---

### [MOVED TO GH ISSUE #72] 2.2 Bundle Size Optimization
**Effort:** ~2-4 hours
**Status:** [ ]
**Depends on:** 2.1
**GitHub:** https://github.com/AugurProject/augur-reboot-website/issues/72

> **Note:** This task has been moved to GitHub issue #72 for team input. Requires test coverage before implementation.

~~Migrate from `ethers.js` (large) to `viem` (tree-shakeable, smaller bundle)~~

| Step | Action |
|------|--------|
| 2.2a | Install `viem` as dependency |
| 2.2b | Update `scripts/calculate-fork-risk.ts` to use viem |
| 2.2c | Remove `ethers` from dependencies |
| 2.2d | Verify build size improvement |

---

### [ ] 2.3 Font Loading Optimization
**Effort:** ~30 min
**Status:** [ ]

**Current:** Google Fonts loaded via `<link>` (blocking)

| Step | Action |
|------|--------|
| 2.3a | Self-host fonts (Handjet, Oxanium) in `public/fonts/` |
| 2.3b | Update `global.css` to use local fonts |
| 2.3c | Add `font-display: swap` to prevent FOIT |
| 2.3d | Remove Google Fonts `<link>` from `Layout.astro` |

---

## Priority 3: Developer Experience

### [ ] 3.1 Add TypeScript Verification
**Effort:** ~15 min
**Status:** [ ]

| Step | Action |
|------|--------|
| 3.1a | Add `typescript` to devDependencies |
| 3.1b | Update `package.json` scripts to use local tsc |
| 3.1c | Run `npm run typecheck` and fix any errors |

---

### [ ] 3.2 Upgrade GitHub Actions
**Effort:** ~15 min
**Status:** [ ]

- Upgrade `actions/upload-pages-artifact` from v3 to v4
- Upgrade `actions/download-artifact` from v4 to v4 (consistent)

---

### [ ] 3.3 Add Error Boundaries
**Effort:** ~30 min
**Status:** [ ]

| Step | Action |
|------|--------|
| 3.3a | Create `ErrorBoundary.tsx` component |
| 3.3b | Wrap ForkGauge in error boundary |
| 3.3c | Add fallback UI for graceful degradation |

---

## Priority 4: SEO & Accessibility

### [ ] 4.1 Reduced Motion Support
**Effort:** ~20 min
**Status:** [ ]

Add `prefers-reduced-motion` detection in CSS and JavaScript:
- Disable/reduce CSS animations
- Skip boot sequence intro automatically
- Respect OS accessibility settings

---

### [ ] 4.2 Expanded Structured Data
**Effort:** ~30 min
**Status:** [ ]

**Current:** Only `Organization` schema

| Step | Action |
|------|--------|
| 4.2a | Add `WebSite` schema with search action to homepage |
| 4.2b | Add `Article` schema to blog posts |
| 4.2c | Add `BreadcrumbList` to all pages |

---

## Priority 5: Architecture & Testing

### [ ] 5.1 State Management Documentation
**Effort:** ~1 hour
**Status:** [ ]

Document the current architecture:
- Why Nanostores for UI state?
- Why React Context for fork data?
- When to use which approach

---

### [ ] 5.2 Add Testing Framework
**Effort:** ~2-3 hours
**Status:** [ ]

| Step | Action |
|------|--------|
| 5.2a | Add Vitest to devDependencies |
| 5.2b | Add @testing-library/react |
| 5.2c | Add test script to package.json |
| 5.2d | Write tests for ForkGauge calculations |
| 5.2e | Write tests for ForkDataProvider |

---

## Task Dependencies

```
1.1 Fix Lint Errors
1.2 Remove Duplicate CSS
1.3 Remove Dead Code
   └─► 2.1 React Optimization
         └─► 2.2 (→ GH #72, deferred)

3.1 TypeScript Verification
   └─► 3.3 Error Boundaries

4.1 Reduced Motion
   └─► (independent)

5.2 Testing Framework
   └─► (depends on 2.1 for testable components)
```

---

## Progress Tracking

| Priority | Tasks | Completed |
|----------|-------|-----------|
| P1: Quick Wins | 1.1, 1.2, 1.3 | 3/3 ✅ |
| P2: Performance | 2.1, 2.2→#72, 2.3 | 0/3 |
| P3: DevX | 3.1, 3.2, 3.3 | 0/3 |
| P4: SEO/A11y | 4.1, 4.2 | 0/2 |
| P5: Arch/Tests | 5.1, 5.2 | 0/2 |
| **Total** | **12 (+1 GH issue)** | **3/12** |
